package ${daoPkg};

import ${modelPkg}.${ClassName};
import org.springframework.stereotype.Repository;

/**
 * ${comments} DAO接口
 * @author ${author} on ${date}
 * @version ${version}
 * @since ${since}
 */
@Repository
public interface ${ClassName}Dao extends CrudDao<${ClassName}, String> {

}